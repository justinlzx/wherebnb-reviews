export class Review {
    constructor(reviewId, listingId, review, userId) {
            this.reviewId = reviewId;
            this.listingId = listingId;
            this.review = review;
            this.userId = userId;
    }
}